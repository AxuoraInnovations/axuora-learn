# SECURITY: Never commit .env or .env.local. Only .env.example (this file) is safe to commit.
# Add real values in .env.local (local) or in your host's env config (e.g. Vercel).

# Contact form: form submissions are sent to your inbox via Formspree.
# 1. Go to https://formspree.io and sign up.
# 2. Create a new form and set the email to axuorainnovations@gmail.com (or your inbox).
# 3. Copy the form ID from the form endpoint (e.g. https://formspree.io/f/xyzabc → xyzabc).
# 4. Add it below. Rename this file to .env.local for local development.
NEXT_PUBLIC_FORMSPREE_FORM_ID=

# Production site URL (for search/social meta tags and favicon). Example:
# NEXT_PUBLIC_SITE_URL=https://axuoralearn.com
NEXT_PUBLIC_SITE_URL=

# Waitlist: Google Apps Script URL. In Vercel use key WAITLIST_URL (type it by hand to avoid invalid-character errors).
# WAITLIST_URL=https://script.google.com/macros/s/YOUR_ID/exec
WAITLIST_URL=

# Supabase (auth). Get from Dashboard → Project Settings → API.
# NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# AI Chat: Bytez API key (dashboard AI Chat uses openai/gpt-oss-20b via Bytez). Get a key at https://bytez.com/settings
# Add to .env.local as BYTEZ_API_KEY=your_key_here
BYTEZ_API_KEY=

# YouTube: when user says "help", "learn", "explain" or "understand", we search for videos. Get a key at https://console.cloud.google.com/apis/credentials — create an API key and enable "YouTube Data API v3" for the project.
YOUTUBE_API_KEY=

# Google Calendar (Study Plan): so students can add their study plan to Google Calendar.
# 1. Google Cloud Console: create OAuth 2.0 Client ID (Web application), enable Google Calendar API.
# 2. Under "Authorized redirect URIs" add the EXACT URIs you will use (no trailing slash):
#      http://localhost:3000/api/auth/google/callback   (local)
#      https://axuoralearn.com/api/auth/google/callback  (production)
#    If you get "Error 400: redirect_uri_mismatch": on the Google error page click "error details",
#    copy the redirect_uri shown there, and add it exactly to Authorized redirect URIs.
# 3. Set GOOGLE_CALENDAR_REDIRECT_URI to the same origin you're opening the app from
#    (e.g. http://localhost:3000 — not 127.0.0.1 — if you open http://localhost:3000).
# 4. If the OAuth consent screen is in "Testing", add your Google account as a test user.
# GOOGLE_CLIENT_ID=your_client_id
# GOOGLE_CLIENT_SECRET=your_client_secret
# Local:  GOOGLE_CALENDAR_REDIRECT_URI=http://localhost:3000/api/auth/google/callback
# Deploy: GOOGLE_CALENDAR_REDIRECT_URI=https://axuoralearn.com/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALENDAR_REDIRECT_URI=http://localhost:3000/api/auth/google/callback
